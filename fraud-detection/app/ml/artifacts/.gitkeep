# ML model artifacts directory.
# fraud_model.pkl and scaler.pkl are generated by running:
#   python app/ml/training/train.py
# or automatically during Docker build (see Dockerfile).
# This file ensures the directory is tracked by version control.
